# Darts Checkout App Release

## 概要

ダーツのチェックアウト練習をより使いやすくするため、アプリ内の設定機能とアップデート案内を追加しました。
また、配布用APKやビルド生成物の扱いを整理し、今後のリリース管理をしやすくしました。

## 主な変更点

- アプリ起動時に新しいバージョンを確認する機能を追加
- GitHub Releases 上の `latest.json` を使って、最新版APKとリリースノートへ誘導できるように変更
- アップデートがある場合に、アプリ内で更新ダイアログを表示
- ダーツ盤の色を変更できる設定を追加
- 変更したダーツ盤カラーを端末内に保存するように対応
- README にAPKのインストール方法とRelease APKの作成手順を追加
- `.gradle/`、`build/`、`app/build/`、`app/release/`、APK/AABなどのビルド生成物をGit管理から除外
- すでにGitに入っていたビルド生成物を追跡対象から削除

## 注意事項

今回のAPKは署名キーを変更してビルドしています。
そのため、以前のAPKがインストールされている端末では、そのまま上書きインストールできない場合があります。

上書きインストールに失敗する場合は、以下の手順で対応してください。

1. 端末から以前のバージョンのアプリをアンインストールする
2. このリリースのAPKをインストールする

アンインストールすると、端末内に保存されているアプリデータや設定が削除される可能性があります。
必要な情報がある場合は、事前に確認してください。

## インストール方法

1. このリリースに添付されている `app-release.apk` をダウンロード
2. Android端末でAPKを開く
3. 必要に応じて「不明なアプリのインストール」を許可
4. 画面の案内に従ってインストール

## 補足

Google Play外で配布されるAPKのため、インストール時にAndroidの警告が表示される場合があります。
これはAPKを直接配布している場合に表示される通常の警告です。
